export type Make = {
  make_ID: number;
  make_Name: string;
};

export type VehicleType = {
  vehicleTypeId: number;
  vehicleTypeName: string;
};

export type Model = {
  model_ID: number;
  model_Name: string;
};